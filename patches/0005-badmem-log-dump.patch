--- a/app/main.c
+++ b/app/main.c
@@ -732,6 +732,11 @@ void main(void)
         start_pass = true;
         if (!dummy_run) {
             display_pass_count(pass_num);
+            // Dump accumulated bad pages in badmem.txt format so the user
+            // can photograph the screen and transcribe them.
+            extern void badmem_log_dump(void);
+            if (pass_num > 0)
+                badmem_log_dump();
             if (error_count == 0) {
                 display_status("Pass   ");
                 display_big_status(true);
