--- a/app/main.c
+++ b/app/main.c
@@ -261,6 +261,10 @@ void main(void)
     display_init();

     error_init();
+
+    /* Dump IMC config + board detection to message area once at startup. */
+    extern void board_calibrate(void);
+    board_calibrate();

     cpu_temp_init();
